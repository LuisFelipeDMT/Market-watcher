fastapi==0.137.2
uvicorn[standard]==0.34.0
starlette==1.3.1
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
python-dotenv==1.2.2
playwright==1.49.1
# Enables SESSION_CIPHER=fernet (encrypted session cache). Optional: the app
# runs without it on SESSION_CIPHER=none/command.
cryptography==43.0.3

# dev / test
pytest==9.1.0
pytest-asyncio==1.4.0
# Dependency CVE audit (scripts/audit.sh).
pip-audit==2.7.3
